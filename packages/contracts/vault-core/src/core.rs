//! `VaultCore`'s single public-methods impl block. Stub for Plan 01 Task 1 (empty, so the
//! `#[entrypoint]` macro's `Router` bound is satisfied) — filled in Task 3 with
//! `init`/`add_adapter`/`deposit`, mirroring `vault-adapter/src/adapter.rs`'s shape.

use stylus_sdk::prelude::*;

use crate::VaultCore;

#[public]
impl VaultCore {}
